#ifndef definitions_h
#define definitions_h

//define enum to set season
enum Season {_winter_, _summer_, _none_};

//define enum to set input or output mode
enum Mode {_input_, _output_};

//define enum for a status device
enum State{_ON_, _OFF_};

//define enum for a valve position
enum Position{_A_, _B_};

#endif